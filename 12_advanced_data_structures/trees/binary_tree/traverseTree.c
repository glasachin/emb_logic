#include"headers.h"
#include"declarations.h"
#include"structures.h"

void** traverseTree(void **arg)
{
    Node *root;
    int choice;
    printf("%s: Begin\n", __func__);

    

    printf("%s: End \n", __func__);
    return arg;
}