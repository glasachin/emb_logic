#include"headers.h"
#include"declarations.h"
#include"structures.h"

int main()
{
    Node *avlTree;

    printf("%s: Start\n", __func__);

    init();

    while(1)
    {
        avlTree = (Node*)(fptr[0])((void*)avlTree);
    }

    printf("%s: End\n", __func__);

    return 0;
}